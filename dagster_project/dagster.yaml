# ====================================================================================
# DAGSTER INSTANCE CONFIGURATION
# ====================================================================================
# This file defines the configuration for the Dagster instance.

storage:
  sqlite:
    base_dir: .dagster_home/storage

run_coordinator:
  module: dagster.core.run_coordinator
  class: QueuedRunCoordinator
  config:
    # Allow long-running operations without timeout
    max_concurrent_runs: 1

run_launcher:
  module: dagster.core.launcher
  class: DefaultRunLauncher
  config:
    # Increase timeout for long-running operations (in seconds)
    # Set to None or a large value for no timeout
    run_timeout: 86400  # 24 hours

compute_logs:
  module: dagster.core.storage.compute_log_manager
  class: LocalComputeLogManager
  config:
    base_dir: .dagster_home/compute_logs

local_artifact_storage:
  module: dagster.core.storage.root
  class: LocalArtifactStorage
  config:
    base_dir: .dagster_home/artifacts

telemetry:
  enabled: false

# Code server heartbeat configuration for long-running operations
code_servers:
  local_startup_timeout: 180
  heartbeat_ttl: 120
