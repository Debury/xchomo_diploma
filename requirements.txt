# ====================================================================================
# PHASE 1 & 2: DATA ACQUISITION & TRANSFORMATION
# ====================================================================================

# Production dependencies
xarray>=2023.10.0
netCDF4>=1.6.0
h5netcdf>=0.15.1
h5py>=3.8.0
pandas>=2.0.0
numpy>=1.24.0

# Climate data retrieval
cdsapi>=0.7.7

# HTTP requests
requests>=2.28.0

# Logging and progress
tqdm>=4.65.0

# Environment management
python-dotenv>=1.0.0

# Geospatial/raster formats
rasterio>=1.3.0
cfgrib>=0.9.10

# Dask for chunked parallel computation
dask[array]>=2023.0.0

# Parquet support
pyarrow>=9.0.0

# Configuration parsing
pyyaml>=6.0

# Date/time utilities
python-dateutil>=2.8.0

# ====================================================================================
# PHASE 3: EMBEDDING GENERATION & VECTOR DATABASE
# ====================================================================================

# Sentence transformers for embedding generation (ONNX deps installed separately in Dockerfile)
sentence-transformers>=5.0.0

# Qdrant for vector database (matches server v1.17.0)
qdrant-client>=1.17.0

# gRPC transport for Qdrant (1.5-2x faster batch upserts vs REST)
grpcio>=1.60.0

# ====================================================================================
# PHASE 4: ORCHESTRATION & WEB UI
# ====================================================================================

# Dagster orchestration framework (pin major version to avoid breaking changes)
dagster>=1.5.0,<2.0.0
dagit>=1.5.0,<2.0.0
dagster-webserver>=1.5.0,<2.0.0

# FastAPI web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Dagster GraphQL client
dagster-graphql>=1.5.0,<2.0.0

# HTTP client for API calls
httpx>=0.25.0

# Pydantic for data validation
pydantic>=2.0.0

# Multipart form support for OAuth2PasswordRequestForm
python-multipart>=0.0.6

# Authentication: argon2id password hashing + JWT tokens
pwdlib[argon2]>=0.2.0
PyJWT>=2.8.0

# ====================================================================================
# PHASE 5: DYNAMIC SOURCE MANAGEMENT & DATABASE
# ====================================================================================

# SQLAlchemy for source store database
sqlalchemy>=2.0.0

# PostgreSQL driver (optional, SQLite is default)
psycopg2-binary>=2.9.0

# Additional ML/NLP dependencies
# PyTorch with CUDA support (GPU acceleration)
# For CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0

# Typing extensions for better type hints
typing-extensions>=4.5.0

# ====================================================================================
# CATALOG BATCH PROCESSING
# ====================================================================================

# Excel file reading (for D1.1.xlsx climate dataset catalog)
openpyxl>=3.1.0

# Cron expression parsing (for per-source scheduling)
croniter>=1.3.0

# Copernicus Marine Service data access (SST datasets)
copernicusmarine>=1.0.0

# ====================================================================================
# RAG EVALUATION (RAGAS framework + LLM judge)
# ====================================================================================

# RAGAS evaluation framework (faithfulness, context precision/recall, answer relevancy)
ragas>=0.2.0,<0.3.0

# LangChain OpenAI wrapper for RAGAS LLM judge (OpenRouter-compatible)
langchain-openai>=0.3.0

# HuggingFace datasets (required by RAGAS internals)
datasets>=2.14.0

# LangChain HuggingFace wrapper (RAGAS embeddings integration)
langchain-huggingface>=0.1.0
